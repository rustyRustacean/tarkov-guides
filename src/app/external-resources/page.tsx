import { ExternalResourcesPage } from "@/features/external-resources/components/ExternalResourcesPage";

/** Route entry for the External Resources feature - thin, delegates to the feature component. */
export default function ExternalResourcesRoute() {
  return <ExternalResourcesPage />;
}
