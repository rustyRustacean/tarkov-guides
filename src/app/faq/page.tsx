import { FAQPage } from "@/features/faq/components/FAQPage";

/** Route entry for the FAQ feature: thin, delegates to the feature component. */
export default function FAQRoute() {
  return <FAQPage />;
}
