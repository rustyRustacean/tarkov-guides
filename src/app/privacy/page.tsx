import { PrivacyPage } from "@/features/privacy/components/PrivacyPage";

/** Route entry for the Privacy feature: thin, delegates to the feature component. */
export default function PrivacyRoute() {
  return <PrivacyPage />;
}
