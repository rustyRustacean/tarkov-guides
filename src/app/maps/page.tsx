import { MapsPage } from "@/features/maps/components/MapsPage";

/** Route entry for the Maps feature - thin, delegates to the feature component. */
export default function MapsRoute() {
  return <MapsPage />;
}
