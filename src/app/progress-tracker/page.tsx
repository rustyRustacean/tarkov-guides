import { ProgressTrackerPage } from "@/features/progress-tracker/components/ProgressTrackerPage";

/** Route entry for the Progress Tracker feature: thin, delegates to the feature component. */
export default function ProgressTrackerRoute() {
  return <ProgressTrackerPage />;
}
