import { CreditsPage } from "@/features/credits/components/CreditsPage";

/** Route entry for the Credits feature - thin, delegates to the feature component. */
export default function CreditsRoute() {
  return <CreditsPage />;
}
